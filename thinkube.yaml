apiVersion: thinkube.io/v1
kind: ThinkubeDeployment

spec:
  deployment:
    type: app

  containers:
    - name: seaweedfs-gateway
      build: .
      port: 8080
      size: small
      health: /api/health
      test:
        enabled: false

  routes:
    - path: /
      to: seaweedfs-gateway
